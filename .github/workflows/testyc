name: Test Fetch

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: |
          curl -v \
            -A "Mozilla/5.0" \
            "https://www.wya6.cn/tv/yc.json"
